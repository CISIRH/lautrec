---
layout: layouts/page.njk
title: Plan du site

---
{# List every content page in the project #}
<ul data-pagefind-ignore>
  {% for entry in collections.allSortedByPathAsc %}
    {% if entry.url %}
      <li><a href="{{ entry.url }}"><code>{{ entry.title }}</code></a></li>
    {% endif %}
  {% endfor %}
</ul>
